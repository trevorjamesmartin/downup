{
  "name": "downup",
  "version": "1.0.0",
  "description": "markdown to markup",
  "main": "index.js",
  "type": "commonjs",
  "engines": {
    "node": ">=20.11.0",
    "npm": ">= 10.2.4"
  },
  "scripts": {
    "test": "node --experimental-vm-modules node_modules/jest/bin/jest.js --verbose", 
    "build": "esbuild index.js --bundle --minify --format=esm --outfile=vendor.mjs",
    "start": "npm run build && npx serve ."
  },
  "keywords": [
    "markdown"
  ],
  "author": "trevor.j.martin@gmail.com",
  "license": "ISC",
  "devDependencies": {
    "esbuild": "0.20.0",
    "jest": "^29.7.0",
    "serve": "^14.2.1"
  }
}
